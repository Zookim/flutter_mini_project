import 'package:flutter/material.dart';

const backgroundColor = Color(0xFF0E0E0E);
const primaryColor = Colors.white;
final secondaryColor =
    Colors.grey[600]; //600이라는 키값 때문에 런타임 색상이되므로 const를 사용할수 없다.